mod code1;
mod code2;
mod code3;
fn main() {
    /*
    part 1 :

    let spaces = "    ";
    let spaces = spaces.len();
    print!("length :{}" , spaces); // gives out the spaces number , here 4


    let mut spa = "  ";
    spa = spa.len(); // here mut has variable type string and we didn't use 'let spa = spa.len()' , so its variable type didnt change to int .
    println!("length : {}" , spa); // mismatched types expected `&str`, found `usize`

    let spa = spa.len(); // this will output correct coz we changed the variable type
    println!("length : {}" , spa); 

     */

       code1::main();
       code2::main();
       code3::main();
}
